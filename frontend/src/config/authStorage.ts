/** Phase 1 + Phase 2 Google login both persist the app JWT here (`/api/v1/auth/google` response). */
export const IKSHAN_AUTH_TOKEN_KEY = 'ikshan-auth-token';

/** Phase 1 chat restores open thread when these are set (same keys as `ChatBotNew`). */
export const IKSHAN_USER_NAME_KEY = 'ikshan-user-name';
export const IKSHAN_USER_EMAIL_KEY = 'ikshan-user-email';
