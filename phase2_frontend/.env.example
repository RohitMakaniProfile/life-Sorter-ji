# Enable skill-call debug logs in the browser console (skill name, input, output).
# When true, each skill call is logged when events are received; logs persist after the agent finishes.
VITE_SKILL_DEBUG_LOGS=false
